[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "phantom"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "yfinance>=0.2",
    "httpx>=0.27",
    "numpy>=1.24",
    "pandas>=2.0",
    "requests>=2.31",
]
